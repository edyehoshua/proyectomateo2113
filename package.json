{
  "name": "proyectomateo2113",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 2113",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 2113",
    "pages:dev": "bun run build && wrangler pages dev dist --port 2113",
    "deploy": "bun run build && wrangler pages deploy dist --project-name proyectomateo2113"
  },
  "devDependencies": {
    "vite": "latest",
    "wrangler": "latest"
  }
}
